# 🛡️ الدليل المعماري والتقني الشامل: Network Sniper Pro
**المرجع الأكاديمي لطلاب أمن المعلومات والشبكات**
**المطور:** Ali-Alqawas

---

## 📖 مقدمة
تم تصميم **Network Sniper Pro** ليكون أداة متقدمة لمراقبة الشبكات والتدقيق الأمني. يعتمد المشروع على مجموعة من التقنيات القوية التي تتعامل مع طبقات الشبكة المختلفة (OSI Model)، بدءاً من طبقة ربط البيانات (Data Link Layer) وصولاً إلى طبقة التطبيقات (Application Layer).

هذا المستند يشرح ميكانيكية عمل كل أداة داخل النظام بالتفصيل، ليكون مرجعاً تقنياً لطلاب أمن المعلومات لفهم كيفية استغلال أو حماية الشبكات.

---

## 🔍 1. استكشاف الشبكة والبصمة (Network Discovery & Fingerprinting)
**الأدوات المستخدمة:** `Nmap`, `python-nmap`

### كيف تتم العملية؟
بدلاً من الاعتماد على استعلامات Ping البسيطة التي تتجاهلها جدران الحماية، يستخدم التطبيق **Nmap** لإجراء مسح شامل (Scan) للشبكة:
1. **ARP Ping Sweep:** يرسل طلبات `ARP Who-has` لجميع عناوين الـ IP في النطاق المحلي. هذه الطريقة تضمن اكتشاف الأجهزة حتى وإن كانت تحظر بروتوكول ICMP.
2. **استخراج الـ MAC Address:** من خلال ردود الـ ARP، يقرأ النظام عناوين الـ MAC.
3. **تحديد الشركة (Vendor Lookup):** يقوم Nmap بمطابقة أول 3 بايتات من الـ MAC Address (والتي تمثل OUI) مع قاعدة بيانات IEEE لمعرفة اسم الشركة المصنعة (مثل Apple, Samsung).
4. **بصمة نظام التشغيل (OS Fingerprinting):** يرسل حزم TCP/UDP غير قياسية ويحلل الردود لتخمين نظام التشغيل بناءً على كيفية تعامل الـ TCP/IP Stack الخاص بالهدف مع الحزم.

---

## ✂️ 2. محركات عزل الأجهزة (Disconnection Engines)
يحتوي النظام على 3 استراتيجيات هجومية لقطع الإنترنت، كل منها يعمل في بيئة مختلفة:

### أ. هجوم تسميم بروتوكول حل العناوين (ARP Spoofing)
**الأدوات المستخدمة:** مكتبة `Scapy` في بايثون.
**البيئة المستهدفة:** الشبكات المنزلية أو المؤسسية العادية.

**كيف تتم العملية (تحت الغطاء):**
- بروتوكول ARP يفتقر إلى التحقق من الهوية (Stateless & Unauthenticated).
- يقوم التطبيق ببناء حزم ARP مزيفة (Spoofed ARP Replies) باستخدام `Scapy`.
- **رسالة للهدف:** التطبيق يرسل للهدف حزمة تخبره: *"أنا الراوتر (عنوان الـ IP للراوتر) وهذا هو الـ MAC الخاص بي (الخاص بجهاز المهاجم)"*.
- **رسالة للراوتر:** التطبيق يرسل للراوتر حزمة تخبره: *"أنا الهدف وهذا هو الـ MAC الخاص بي"*.
- **النتيجة:** تصبح حركة مرور الهدف تمر عبر جهاز المهاجم (Man-in-the-Middle). 
- **ضمان القطع:** في النظام قمنا ببرمجة جدار الحماية `iptables` ليقوم بإسقاط (DROP) أي حزم تأتي من الـ MAC الخاص بالهدف، مما يؤدي إلى انقطاع الإنترنت لديه كلياً حتى وإن كان جهاز المهاجم يدعم تمرير البيانات (IP Forwarding).

### ب. هجوم طرد الشبكة (Deauthentication Attack - IEEE 802.11)
**الأدوات المستخدمة:** `Scapy`, `iw`, `ifconfig`
**البيئة المستهدفة:** الشبكات المفتوحة، شبكات الكافيهات، والشبكات المعزولة (AP Isolation).

**كيف تتم العملية (تحت الغطاء):**
- يعمل هذا الهجوم في طبقة الراديو (Wi-Fi). يقوم النظام بتحويل كارت الشبكة إلى وضع المراقبة (Monitor Mode) لالتقاط وحقن الحزم اللاسلكية.
- يقوم `Scapy` بإنشاء حزمة إدارة (Management Frame) من نوع `Deauthentication`.
- يتم إرسال الحزمة محملة برمز سبب الانقطاع (Reason Code 7 - Class 3 frame received from nonassociated STA).
- **التكتيك المزدوج:** يتم إرسال حزم للهدف مدعين أننا الراوتر نطالبه بالخروج، وحزم للراوتر مدعين أننا الهدف نطلب الخروج.
- يتم إرسال هذه الحزم بكثافة (Burst) بمعدل أجزاء من الثانية مما يمنع الهدف من إعادة الاتصال تماماً.

### ج. حجب مستوى الكيرنل (Iptables Blocking)
**الأدوات المستخدمة:** `Netfilter / iptables`
**البيئة المستهدفة:** عندما يكون كمبيوتر المهاجم هو نفسه موزع الإنترنت (Hotspot/Gateway).

**كيف تتم العملية (تحت الغطاء):**
- يقوم النظام بالتخاطب مباشرة مع جدار حماية نواة لينكس (Netfilter).
- يدرج قاعدة في سلسلة المرور `FORWARD` وسلسلة الدخول `INPUT` لرفض أي حزمة تأتي من الـ MAC Address الخاص بالهدف:
  `iptables -I FORWARD -m mac --mac-source <MAC> -j DROP`
- هذه هي الطريقة الأكثر نظافة وفعالية لأنها لا تعتمد على الخداع بل على سلطة الجهاز كمسار عبور.

---

## 🎮 3. وضع الألعاب وتحديد النطاق (Game Mode / Traffic Shaping)
**كيف تتم العملية؟**
عند تفعيل وضع الألعاب، يعطي النظام الأولوية لجهاز اللاعب (مثل كمبيوتر اللاعب أو جهازه المخصص). يعتمد ذلك على آليات التحكم في عرض النطاق الترددي بحيث يتم إعاقة أو إبطاء اتصال الأجهزة الأخرى لمنعها من استهلاك حزم البيانات (Bandwidth) مما يضمن تقليل التأخير (Ping/Latency) بشكل كبير للجهاز المفضل.

---

## 🛡️ 4. فحص المنافذ والخدمات (Port Scanning & Vulnerability Analysis)
**الأدوات المستخدمة:** `Nmap (nmap.PortScanner)`

**كيف تتم العملية؟**
- يستخدم النظام فحص `TCP SYN Scan` (المعروف بـ Half-open scan).
- يرسل حزمة `SYN` للهدف.
- إذا استلم `SYN/ACK`، فهذا يعني أن المنفذ مفتوح (Open).
- يرسل فوراً حزمة `RST` لقطع الاتصال وعدم تسجيله في سجلات الخادم (Stealthy).
- إذا استلم `RST`، فالمنفذ مغلق.
- إذا لم يستلم شيئاً، المنفذ محمي بـ Firewall (Filtered).

---

## 💾 5. هندسة التطبيق (Software Architecture)
تم بناء المشروع بشكل تركيبي (Modular) لضمان الأداء العالي:
- **المعالجة المتعددة (Multi-threading):** كل هجوم (ARP أو Deauth) يعمل في `Thread` مستقل (Daemon Thread) في الخلفية لكي لا يتجمد واجهة المستخدم (GUI).
- **الذاكرة المستدامة (Persistence):** يستخدم `SQLite` لتسجيل الأجهزة التي تدخل وتخرج من الشبكة، و `JSON` لحفظ الإعدادات وقوائم VIP لمنع فقدانها عند إعادة تشغيل البرنامج.
- **التصميم التفاعلي (UI):** باستخدام مكتبة `customtkinter` لتقديم واجهة رسومية داكنة/فاتحة (Dark Mode) تعطي شعوراً احترافياً وتجربة مستخدم سلسة.

---
**خاتمة:** 
هذا المشروع هو دراسة حالة ممتازة في أمن الشبكات الهجومي والدفاعي، ويوضح كيف يمكن تسخير بروتوكولات الشبكة القديمة (مثل ARP و 802.11 Management Frames) لأغراض التحكم والمراقبة.
